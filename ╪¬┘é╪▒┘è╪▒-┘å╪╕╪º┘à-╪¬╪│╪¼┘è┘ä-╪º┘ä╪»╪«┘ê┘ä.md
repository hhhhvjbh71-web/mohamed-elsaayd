# تقرير إعادة بناء نظام تسجيل الدخول

> النطاق: نظام المصادقة فقط. لم يتغيّر تصميم أي صفحة (الحقول والأزرار كما هي، فقط أُضيف حقل "تذكّرني" وجُعل حقل الهاتف في الملف الشخصي للقراءة فقط)، ولم يتغيّر شكل لوحة التحكم إطلاقاً.

## 1) المشكلة في الكود القديم
كان تسجيل الدخول بالكامل من جهة المتصفح:
- `localStorage.getItem('iraqiplatform_current_user')` هو "الجلسة"، وأي كود جافاسكريبت (أو أي شخص يفتح Developer Tools) يقدر يكتب فيه مباشرة ويصبح أي مستخدم يريد.
- كلمة مرور الأدمن كانت نصاً صريحاً في الكود (`data.js`: `ADMIN_PASSWORD = 'adm123'`) وبوابة الداشبورد في `index.html` كانت تقارن كلمة مرور واحدة مكتوبة (hash بسيط بلا ملح) — لا علاقة لها بحساب حقيقي.
- تسجيل الدخول والتسجيل كانا يبحثان في مصفوفة `getUsers()` المحمّلة بالكامل من Firestore إلى المتصفح (**بما فيها كل كلمات مرور كل المستخدمين نصاً صريحاً**) ويقارنان القيم محلياً.
- **الأخطر:** حارس صفحة الداشبورد (`dashboard.html`) كان يتحقق فقط من `localStorage.getItem('alsaqr_current_user').role === 'admin'` — أي حقن هذا الكائن في الكونسول يفتح لوحة التحكم كاملة بلا أي تحقق من الخادم.

## 2) الحل: Firebase Authentication كمصدر الحقيقة الوحيد
كل التحقق الآن يمر عبر **Firebase Authentication** (كلمات المرور مشفّرة على خوادم Firebase ولا تُخزَّن أبداً كنص في Firestore أو في المتصفح) + مستند مستخدم في Firestore. رقم الهاتف هو معرّف الدخول، ويُترجم داخلياً إلى بريد إلكتروني وهمي (`01xxxxxxxxx@phone.drmohamed-physics.app`) لأن Firebase Auth يتطلب بريداً — **لا يُرسل له أي شيء**، وهو مجرد معرّف حساب.

**الفصل المطلوب بين المراحل** (كل مرحلة دالة مستقلة في `auth-service.js`):

| المرحلة | الدالة | المصدر |
|---|---|---|
| 1. هل الحساب موجود؟ | `accountExists(phone)` | قراءة `phone_index/{phone}` من Firestore (قراءة واحدة بالمعرّف، بلا تحميل كل المستخدمين) |
| 2. هل كلمة المرور صحيحة؟ | `verifyPassword(phone, pw)` | `signInWithEmailAndPassword` — يتحقق **على خادم Firebase** |
| 3. إنشاء الجلسة | `createSession(fbUser)` | تحميل `users/{uid}` (كلمة المرور محذوفة من الكائن دائماً) |
| 4. استعادة الجلسة | `restoreSession()` | `onAuthStateChanged` + إعادة تحقق من الخادم (`user.reload()`) |

`login()` يستدعي الأربعة بالترتيب: رقم غير موجود → توقف فوراً بلا تجربة كلمة مرور ("رقم الهاتف غير مسجل")؛ رقم موجود → `verifyPassword` ("كلمة المرور غير صحيحة" إن فشلت)؛ نجحت → `createSession`.

## 3) الجلسة
- Firebase Auth يحفظ الجلسة بنفسه (توكن موقّع من الخادم) وتبقى بعد إغلاق المتصفح — هذا هو `localStorage.setPersistence(LOCAL)` (الافتراضي، أو `SESSION` إذا أُلغي تحديد "تذكّرني").
- `localStorage['iraqiplatform_current_user']` أصبح **كاشاً للعرض فقط**: عند فتح الصفحة تُستعاد الجلسة من Firebase أولاً، ثم يُعاد تحميل الملف الشخصي من Firestore، وأي كاش قديم/مزوَّر يُستبدَل بالبيانات الحقيقية تلقائياً (مُختبر: تعديل الكاش يدوياً لا يغيّر الحساب الفعلي).
- تبديل الحساب على نفس الجهاز يمسح كل بيانات الحساب السابق المخزّنة محلياً (نتائج الاختبارات المؤقتة، تقدّم الدروس) حتى لا تظهر لحساب آخر.

## 4) لوحة التحكم
- حارس الداشبورد القديم (localStorage فقط) **حُذف بالكامل**. الحارس الجديد: `AuthService.requireAdmin()` — يتحقق من جلسة Firebase حقيقية **و** من وجود مستند `admins/{uid}` (لا يمكن لأي عميل كتابته — فقط عبر أداة الإدارة `auth-admin.js`).
- بوابة "🎓 لوحة التحكم" في الصفحة الرئيسية أصبحت **هاتف + كلمة مرور** حقيقيين بدل كلمة مرور واحدة مكتوبة في الكود، وتُرفض حتى لو كانت بيانات طالب حقيقي وصحيح (ليس أدمن).
- زر الخروج يستخدم `AuthService.logout()` (يُنهي جلسة Firebase فعلياً، لا يكتفي بمسح المتصفح).

## 5) نقاط أخرى أُصلحت في نفس السياق
- **`firebase-service.js`** لم يعد يُنزّل كل حسابات المستخدمين (بكلمات مرورهم) إلى متصفح أي زائر — هذه الوظيفة الآن للأدمن فقط، وحتى معه تُحذف كلمة المرور من أي كائن قبل عرضه.
- **رقم الهاتف لا يُغيَّر من المتصفح**: حقل الهاتف في الملف الشخصي أصبح للقراءة فقط، و`updateProfile()` لا يقبل حقل الهاتف حتى لو أُرسل مباشرة عبر الكود (جُرِّب: استدعاء مباشر لتغييره يُتجاهَل).
- **تغيير كلمة المرور** أصبح عبر إعادة مصادقة حقيقية (`reauthenticateWithCredential`) بدل مقارنة نص محلي.
- إزالة تصادم بين نسختين من صفحتي الدخول والتسجيل: `templates.js` يحمل نسخة فعلية تُستخدم في العرض، وكانت نسخة `app.js` القديمة (fallback) هي الوحيدة التي تُحدَّث سابقاً — **أضفت الحقل الناقص لكلتا النسختين** حتى لا يتكرر هذا الفخ مستقبلاً.

## 6) خطوات التفعيل (مطلوبة قبل النشر — الموقع الجديد لن يعمل بدونها)
تفصيلها في `README-auth.md`:
1. فعّل Email/Password في Firebase Console (Authentication → Sign-in method).
2. شغّل `node auth-admin.js migrate --key ./serviceAccount.json` لنقل كل حساب موجود إلى Firebase Authentication **بنفس معرّفه القديم** (فتبقى نتائج الاختبارات وتقدّم الدروس مربوطة به تلقائياً) وبإنشاء `phone_index`. يدعم `--dry-run` للمعاينة أولاً.
3. اضبط حساب الأدمن بكلمة مرور جديدة قوية عبر `--admin-password` في نفس الأمر (الأداة **ترفض** استخدام كلمة المرور الافتراضية القديمة `adm123` تلقائياً حماية من نسيانها).
4. ادمج `firestore.rules.auth-snippet.txt` في قواعد Firestore عندك وانشرها.
5. بعد التأكد أن الجميع قادرون على الدخول: شغّل نفس أمر `migrate` مع `--strip-passwords` لمسح كلمات المرور القديمة (النص الصريح) المتبقية في `users`/`students`.
6. أداة `reset-password` و`make-admin` و`remove-user` متاحة لإدارة الحسابات لاحقاً (تفاصيلها في الأداة نفسها).

ملفا الإدارة `auth-admin.js` و`firestore.rules.auth-snippet.txt` **لن يُرفعا مع الموقع** (مضافان إلى `firebase.json` → `hosting.ignore`) — شغّله من جهازك فقط، ولا ترفع ملف مفتاح الخدمة (`serviceAccount.json`) لأي مكان.

## 7) الاختبارات
82 فحصاً آلياً على متصفح حقيقي (Chromium)، منها 52 خاصة بتسجيل الدخول تحديداً:

| السيناريو | النتيجة |
|---|---|
| رقم غير مسجَّل | رسالة "رقم الهاتف غير مسجل"، بلا تجربة كلمة مرور، بلا إنشاء حساب |
| رقم مسجَّل + كلمة مرور خاطئة | "كلمة المرور غير صحيحة"، الحقل يُمسح لإعادة المحاولة |
| رقم مسجَّل + كلمة مرور صحيحة | دخول ناجح، البيانات من قاعدة البيانات فعلياً |
| خروج ثم دخول مرة أخرى | يعمل بشكل صحيح |
| إغلاق المتصفح وإعادة فتحه | الجلسة باقية، وتُعاد التحقق من الخادم عند الفتح |
| تسجيل حساب جديد، ثم رقم مستخدم من قبل | الأول ينجح، الثاني يُرفض بوضوح |
| تعديل الكاش المحلي لانتحال حساب آخر | يُرفض تلقائياً؛ الحساب الحقيقي يستبدل التلاعب |
| جلسة مزوَّرة كاملة بلا حساب Firebase حقيقي | مرفوضة، إعادة توجيه لصفحة الدخول |
| "تذكّرني" مطفأة + جلسة متصفح جديدة | لا تُستعاد الجلسة |
| بيانات الحساب الأول لا تظهر للحساب الثاني | مُختبر صراحة — نظيف تماماً |
| طالب يفتح `dashboard.html` مباشرة | يُرفض ويُعاد توجيهه |
| كاش أدمن مزوَّر بلا جلسة Firebase حقيقية | يُرفض ويُمحى |
| أدمن حقيقي يفتح لوحة التحكم | يعمل، والخروج يعمل |
| انقطاع الشبكة أثناء الدخول | رسالة شبكة واضحة، ليست "كلمة مرور خاطئة" |
| تغيير رقم الهاتف مباشرة عبر استدعاء API | يُتجاهَل، الرقم في قاعدة البيانات لا يتغيّر |
| كل صفحات الموقع (رئيسية، كورسات، بروفايل...) | تعمل بلا أخطاء جافاسكريبت، مسجَّلاً ومسجَّلاً خروجه |
| نظام الاختبارات (من التقرير السابق) | أُعيد تشغيله بالكامل بعد نظام الدخول الجديد — 44/44 |

**حدّ الاختبار:** بيئتي بلا وصول إلى Firebase الحقيقي، فالفحوص على Authentication وFirestore **محاكيين** بواجهة برمجية مطابقة (بما فيها معاملات التزامن). لم أختبر على Firebase الحقيقي ولا على قواعد الأمان الفعلية بعد نشرها — نفّذ خطوات القسم 6 على نسخة تجريبية أولاً.

## 8) ما لم يتغيّر
تصميم الصفحات، بنية بيانات الكورسات والدروس، نظام الاختبارات، قاعدة البيانات (لم تُحذف أو تُعدَّل أي بيانات موجودة — فقط أُضيفت مجموعات جديدة: `phone_index`، `admins`).
