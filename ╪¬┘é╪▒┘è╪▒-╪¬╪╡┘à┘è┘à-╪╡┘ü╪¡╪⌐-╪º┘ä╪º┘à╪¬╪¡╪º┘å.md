# تقرير: إعادة تصميم واجهة صفحة الامتحان

## المشكلة اللي طلبت حلها
عند اختيار إجابة ثم تحريك الماوس فوق إجابة تانية، تأثير الـ hover كان بيخلي الشكل يبدو
وكأن الاختيار اتغيّر، رغم إن الاختيار الفعلي (في الكود) مكنش بيتغيّر.

## السبب (بعد الفحص)
الاختيار فعليًا كان بيتسجّل صح 100% في JavaScript (متغيّر `_answers[qi]` + كلاس `.selected`
بيتحطوا فعليًا عند الضغط بس، مش عند الـ hover — الكود ده كان سليم من الأساس ومحتاجش تغيير).
المشكلة كانت بصرية بحتة في الـ CSS: حالة `:hover` وحالة `.selected` كانتا بنفس تقريبًا لون
الخلفية (أزرق فاتح جدًا) واختلاف بسيط جدًا في لون الحدود، فبصريًا كانوا شبه بعض تمامًا،
فكان يبدو للعين إن الاختيار "بيتحرك" مع الماوس رغم إنه مش بيتحرك فعليًا.

## الحل
أعدت تصميم CSS الخاص بصفحة الامتحان بالكامل (`app.js` — دالة `renderTestPage`) مع
**عدم لمس أي سطر JavaScript خاص بمنطق الاختيار أو التصحيح أو حساب النتيجة إطلاقًا** —
ولا أي تغيير في أسماء الدوال أو الـ id الوظيفية (`selectTestOption`, `submitTest`,
`testOpts_X`, `.selected`, `.correct`, `.wrong`... إلخ)، فقط تحسين شكلها.

أهم نقطة: خليت حالة الـ **Hover محصورة صراحةً على العناصر غير المختارة فقط**
(`.test-option:not(.selected):hover`)، وخليت شكلها (رفعة بسيطة + ظل خفيف + حدود فاتحة)
مختلف تمامًا عن شكل `.selected` (حدود برتقالية سميكة + خلفية كريمية + دائرة الحرف ممتلئة
باللون + علامة ✓) — فبقى مستحيل بصريًا الخلط بينهم مهما تحرك الماوس.

## التصميم الجديد
- استخدمت نفس ألوان هوية الموقع الأساسية (البرتقالي `--primary-500/600`) بدل الأزرق اللي
  كان مستخدم في الصفحة دي بس (كان غير متطابق مع باقي المنصة أصلاً).
- كارد السؤال بقى مستقل بصريًا تمامًا عن الاختيارات (ظل، حدود، شريط لوني جانبي).
- شارة "Question X of Y" بقت Badge برتقالي متدرّج بدل نص عادي.
- كل اختيار بقى Card كبير بحرف A/B/C/D جوه دائرة، مع علامة ✓ تظهر بس لما يتحدد.
- أزرار التنقل بين الأسئلة تحت بقت أوضح: **دائري حالي** (ممتلئ برتقالي) مختلف عن
  **مُجاب عليه** (خلفية فاتحة) عن **غير مُجاب** (أبيض عادي).
- الـ Timer وباقي الـ Header زي ما هما تمامًا، بس بألوان أكثر انسجامًا.
- تم اختبار الشكل بمعاينة بصرية فعلية قبل التسليم (حالة Selected وحالة Hover منفصلتين
  تمامًا كما هو مطلوب).

## الملفات اللي اتعدّلت
- **`app.js`** فقط — تعديل CSS داخل `renderTestPage` بالكامل + إضافة عنصر `<span
  class="test-opt-check">` بصري فقط (بيتفعّل تلقائيًا مع نفس كلاس `.selected` الموجود،
  من غير أي منطق جافاسكريبت إضافي).

مفيش أي تغيير في `dashboard.html` أو منطق حساب الدرجة أو حفظ المحاولات.
