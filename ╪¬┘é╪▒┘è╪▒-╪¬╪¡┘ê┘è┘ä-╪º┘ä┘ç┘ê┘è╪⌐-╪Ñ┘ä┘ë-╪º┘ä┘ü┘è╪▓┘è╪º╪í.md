# تقرير تحويل الهوية — منصة الدكتور محمد عبد الله | عميد الفيزياء

**النطاق:** هوية + واجهة + Static Configuration فقط. لا تغيير في قاعدة البيانات ولا في منطق البرنامج.

## 1) ما تغيّر
| المجال | التفاصيل |
|---|---|
| الاسم | كل ظهور لـ «مستر/المهندس محمود عبد الدايم» (الاسم القديم الفعلي في المشروع) و«الأستاذ طلعت الشناوي» و«المهندس حسام العراقي» استُبدل بـ «منصة الدكتور محمد عبد الله / الدكتور محمد عبد الله» و«عميد الفيزياء». في النصوص الإنجليزية: **Dr. Mohamed Abdullah — Dean of Physics** (قابل للتعديل من `data.js` → `SITE_CONFIG` وباقي النصوص). |
| الألوان | تم تطويرها لاحقاً إلى هوية موف/بنفسجي كاملة (انظر القسم 7). |
| اللوجو | حرف M على شكل موجة داخل مدار مع إلكترون. مصدر واحد `physicsLogoMark()` في `data.js` يُستخدم في الهيدر والفوتر وشاشتي الدخول/التسجيل، ونسخة SVG مستقلة `logo-mark.svg`. |
| شاشة التحميل | استُبدلت بالكامل: ذرة بثلاثة مدارات وإلكترونات، موجات تنبعث من النواة، موجة متحركة كمؤشر تقدم، معادلة (F=ma ← V=IR ← E=mc² ← λ=v/f) تظهر مع كل خطوة، ثم الاسم و«عميد الفيزياء». الحد الأدنى للظهور **~2.1 ثانية بدل 6.3**. تدعم الوضع الداكن و`prefers-reduced-motion` ومختبرة من 320px إلى 1280px. |
| الهيدر | الاسم والشعار الجديدان، والسطر «عميد الفيزياء» يظهر الآن على الموبايل أيضاً، والاسم الطويل يلتف على سطرين بدل أن يكسر التصميم. |
| الـHero والصفحات | معادلات فيزيائية طافية بدل معادلات الرياضيات، عنوان الهيرو بالاسم العربي، نصوص إنجليزية، مراحل دراسية بوسوم فيزياء، اختبار تجريبي في الميكانيكا. |
| PWA | `manifest.json` (الاسم، short_name، الوصف، lang/dir، theme `#1D1038`، background `#FAF9FC`)، كل الأيقونات (192/512/maskable/apple-touch/favicon.ico/16/32)، عنوان الصفحة، `theme-color`، `apple-mobile-web-app-title`، `offline.html`، نافذة التثبيت. `id` في الـmanifest بقي `/index.html` عمداً حتى لا يُعامل التطبيق كتطبيق جديد. |
| لوحة التحكم | الاسم والشريط الجانبي (Navy) والأيقونة (كانت صورة ملف غير موجود) وقيم الإعدادات الافتراضية وبرومبتات الذكاء الاصطناعي. |
| الصور | جميع الأيقونات كانت صورة المدرس القديم وتم استبدالها. `teacher-new.jpg` و`hero-teacher.jpg` استُبدلا ببطاقة هوية مؤقتة (الشعار + الاسم) بنفس اسم الملف. |
| الكاش | شُغّل `update-version.sh` → النسخة `20260919-2132` (version.json + `?v=` + sw.js). |

## 2) ما لم يُلمس عمداً (قاعدة البيانات والاتصال)
- `firebase-config.js` و`firebase-service.js`: تغيّر **سطر تعليق واحد فقط** في كل منهما؛ سطور الاتصال مطابقة حرفياً للأصل (تم التحقق آلياً). `firebase.json` مطابق تماماً.
- معرّف مشروع Firebase `mahmoud-abdeldaem-manassa` ونطاق الاستضافة (canonical / sitemap / og:url / JSON-LD): يخصّان البنية التحتية.
- مفاتيح localStorage (`alsaqr_*` و`iraqiplatform_*`) ومعرّفات مثل `iraqi-loader` و`math-grade1-term1` في بيانات العرض التجريبية.
- أي بيانات محفوظة داخل قاعدة البيانات (كورسات، إعدادات، طلاب).

## 3) نقاط تحتاج قرارك
1. **رقم فودافون كاش ورقم واتساب الدفع** ثابتان في `app.js` (السطور 1121–1122 و4225–4229): بعد التحويل سيدفع الطلاب على رقم المدرس القديم/صاحب المنصة السابق باسم الدكتور محمد. **لم أغيّره لأني لا أملك الرقم الجديد.** كذلك `wa.me/201000000000` أرقام placeholder في زر التواصل.
2. **صورة الدكتور:** استبدل `teacher-new.jpg` (يفضّل 3:4، مثلاً 960×1280) وسيظهر تلقائياً في الهيرو وشاشتي الدخول والتسجيل.
3. **بيانات السيرة والإحصاءات** (15+ سنة خبرة، 5000+ طالب، 99% نجاح، آراء الطلاب في `data.js`) نصوص نموذجية موروثة من الهوية القديمة — راجعها مع الدكتور.
4. **قيم داخل قاعدة البيانات ستظل قديمة حتى مرحلة قاعدة البيانات:** `settings.platformName/teacherName` (تظهر في بطاقات الطالب المطبوعة ولوحة الإعدادات إن كانت محفوظة)، ولون `cardNameBg` للكورسات القديمة.
5. **جلسة الأدمن المسجَّل دخوله مسبقاً** تحتفظ باسمها القديم في متصفحه حتى تسجيل خروج ودخول جديد.
6. **المراحل الإعدادية** ما زالت في المنصة (هيكل وليست هوية). ألغِها لاحقاً إن كانت الفيزياء ثانوية فقط.
7. **الاسم بالإنجليزية** (Dr. Mohamed Abdullah) اقتراح مني؛ عدّله إن كان لديهم كتابة رسمية مختلفة.
8. ملفات `التعديلات-المرحلة-*.md` القديمة تحتوي الاسم القديم كسجل تاريخي، وهي منشورة علناً مع الاستضافة لأن `firebase.json` لا يستثني ملفات md (يمكن إضافة `**/*.md` لقائمة ignore).

## 4) ملاحظات أمنية (خارج نطاق هذه المهمة، لم تُعدَّل)
- بوابة لوحة التحكم في `index.html` تتحقق من كلمة المرور في المتصفح ثم تنشئ جلسة أدمن في localStorage، وبيانات أدمن مضمّنة في `data.js`. أي شخص يقرأ الكود يستطيع تجاوزها. الحل الصحيح: Firebase Auth + قواعد Firestore.

## 5) الاختبارات
- فحص بناء الجملة لكل ملفات JS/JSON: ناجح.
- لقطات شاشة (Chromium) للـLoader على 320/390/768/844×390/1280 وبالوضعين الفاتح والداكن، وللهيدر (ومنيو الموبايل) والهيرو وشاشتي الدخول/التسجيل والفوتر ولوحة التحكم: لا كسر في التصميم، ونفس الأخطاء الأساسية الموجودة في الأصل (`COURSES_DATA` بلا Firebase) بدون أخطاء جديدة.
- بحث نهائي شامل: لا أثر ظاهر للاسم القديم أو الرياضيات أو ألوان الهوية القديمة. المتبقي مقصود: معرّف Firebase وروابط الدومين، `Math.*` في JavaScript، اسم طالب تجريبي «محمود سعد» في بيانات لوحة التحكم، ولون التحذير `--warning`.
- **حدّ الاختبار:** بيئتي بلا وصول إلى Firebase، فلم أختبر تسجيل الدخول والكورسات الحيّة. تعديلات JS اقتصرت على نصوص وSVG وألوان وسكربت شاشة التحميل. جرّب على نسخة تجريبية قبل النشر.

## 6) النشر
ارفع كل الملفات إلى الاستضافة (النسخة مختومة `20260919-2132`). عند أي تعديل لاحق شغّل `./update-version.sh`.

## 7) تطوير الهوية البصرية: اتجاه الموف / البنفسجي
عائلة لونية واحدة بدرجات متعددة (لا موف واحد في كل شيء). كل الألوان معرّفة كمتغيرات في `style.css` (`:root` و`PURPLE SYSTEM`).

| الدور | اللون | الكود |
|---|---|---|
| أساسي غامق (الهيرو، الفوتر، الخلفيات الداكنة) | Deep Aubergine | `#1D1038` ← `#2E1A55` ← `#4B2C8C` |
| أساسي (أزرار، روابط، عناوين) | Royal Violet | `#6A3FBD` · تفاعل `#7F56D0` · Hover `#55309A` |
| درجات ثانوية (كروت، أيقونات، Badges) | Indigo · Amethyst · Orchid · Periwinkle | `#6F6AE8` · `#B071E8` · `#A653C4` · `#8A85F0` |
| مساعد فاتح | Lavender / Lilac | `#EEE7FA` · `#C5B0EC` |
| خلفية الموقع / النص | Off-white · Ink | `#FAF9FC` · `#221836` |

- **الهيرو:** بانر أوبرجين عميق بحواف سفلية مدوّرة، النص والأزرار على جهة والصورة على الجهة الأخرى داخل إطار زجاجي مع مدارات وهالة وشرائح معادلات. المحتوى نفسه، وأُعيد ترتيب عناصره فقط (العنوان + الأزرار معاً، الصورة منفصلة).
- **الهيدر:** شريط زجاجي (Glass) مع Hover وحالة Active بشريط تحتي، وأزرار موحّدة. ينقلب إلى قائمة الموبايل عند 1080px بدل 768px كي لا يزدحم على التابلت.
- **التيبوغرافي:** خط العرض Plus Jakarta Sans للعناوين اللاتينية (يُحمَّل من Google Fonts مع بدائل)، وInter للنص، وTajawal للعربية. أحجام العناوين fluid وتباعد الأقسام متناسق.
- **التوحيد:** الأزرار، الكروت (Radius 20px وظلال بنفسجية خفيفة)، الـInputs (Focus بحلقة بنفسجية)، Filter chips، Badges، الأسئلة الشائعة، الفوتر، شاشتا الدخول والتسجيل، لوحة التحكم (الشريط الجانبي والأزرار).
- **اللوجو والأيقونات والـFavicon وبطاقة صورة المدرس المؤقتة وشاشة التحميل و`offline.html`** أُعيد إنتاجها بنفس الهوية.
- **لم يُغيَّر:** أي نص أو وظيفة أو Backend أو قاعدة بيانات، ولا ألوان الحالات الدلالية (نجاح/خطر/تحذير/واتساب/نجوم التقييم).
- **الاختبار:** لقطات على 320/390/820/1440، ولا Overflow أفقي على 7 عروض (320 → 1440) في الرئيسية والدخول والتسجيل.
