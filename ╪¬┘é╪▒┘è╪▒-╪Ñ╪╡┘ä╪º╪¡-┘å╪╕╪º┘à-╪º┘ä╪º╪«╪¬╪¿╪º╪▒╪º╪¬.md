# تقرير إصلاح: الاختبار لا يظهر للطالب

## المشكلة كما وصلتني
الاختبار يتعمل وينربط بالدرس بنجاح من الداشبورد، لكن الطالب لما يدخل المنصة، الاختبار مايظهرش
له خالص.

## السبب الحقيقي (بعد فحص الكود كامل)
المنصة فعليًا مبنية من **جزئين منفصلين** بيشتغلوا كل واحد لوحده:

- **`dashboard.html`** (لوحة التحكم): فيها كل كود إنشاء/تعديل الاختبارات (العادي والذكاء
  الاصطناعي)، وهي فعلًا بتحفظ الاختبار صح في قاعدة بيانات Firebase (Firestore) في
  Collection اسمها `quizzes` — ده تأكدت منه بنفسي في الكود، الحفظ شغال 100%.
- **`index.html` + `app.js` + `dashboard-bridge.js`** (واجهة الطالب): وهي الجزء اللي بيعرض
  الدرس والاختبار للطالب.

المشكلة: **واجهة الطالب أصلاً مفيهاش أي كود بيجيب بيانات الاختبارات من Firebase خالص.**
كل الدوال اللي بتجيب الاختبار (`getQuizById`, `getAllQuizzes`) كانت بتدوّر بس جوه
`localStorage` (ذاكرة المتصفح المحلية). المشكلة في `localStorage` إنه **خاص بكل جهاز/متصفح
على حدة** — مش متزامن بين الأجهزة. يعني:

- لما المعلم يعمل اختبار من `dashboard.html` على جهازه، الاختبار يتحفظ في `localStorage`
  بتاع **جهاز المعلم فقط** (بجانب الحفظ الصحيح في Firebase).
- الطالب بيفتح المنصة من **جهاز تاني تمامًا** (موبايله هو) → `localStorage` بتاعه فاضي
  تمامًا من أي اختبارات → `getQuizById()` بترجع `null` → الشاشة بتظهر رسالة "الاختبار قيد
  التجهيز من المعلم" بدل الاختبار الفعلي.

بمعنى تاني: **الربط بين الدرس والاختبار (`quizId`) كان شغال صح فعلاً** (عشان كده شارة
"Quiz Available" والتاب كانوا ظاهرين في الصور اللي بعتّها) — لكن لما الطالب يضغط عليها،
الاختبار نفسه (الأسئلة، درجة النجاح) مكنش موجود في متصفحه أصلاً.

لقيت كمان تعليق في كود الداشبورد بيوضح إن فيه محاولة سابقة لحل المشكلة دي عن طريق حفظ نسخة
في `iraqiplatform_quizzes` (مفتاح تاني في localStorage) — لكن الحل ده برضه مكنش هيشتغل مع
طالب حقيقي على جهاز مختلف، لأنه برضه localStorage.

## الحل اللي طبّقته
أضفت **قناة مزامنة حقيقية من Firebase مباشرة لواجهة الطالب**، بنفس الطريقة والنمط المستخدم
بالفعل في المشروع لمزامنة الكورسات (نفس الأسلوب اللي عليه `platform_data/courses_list`
تمامًا) — يعني مفيش نظام جديد، امتداد لنفس الطريقة الموجودة:

- **`dashboard-bridge.js`**: أضفت دالة `startQuizzesFirebaseSync()` بتعمل
  `onSnapshot` مباشر على `collection('quizzes')` في Firebase. أي اختبار موجود فعلاً (قديم
  أو جديد، عادي أو AI) بينزل فورًا لأي جهاز طالب فور دخوله المنصة، ويتخزن في
  `iraqiplatform_quizzes` و `alsaqr_quizzes` بنفس الصيغة اللي `app.js` بيقرأها أصلاً
  (مفيش أي تغيير في أسماء الحقول).
- لو الطالب فاتح صفحة الدرس فعلاً واللحظة دي وصل الاختبار، الصفحة بتتحدّث تلقائيًا (من غير
  ما يعمل Refresh يدوي) — إلا لو كان **وسط حل اختبار حي فعلاً** في نفس اللحظة، عشان
  ميتمسحش إجاباته.

هذا هو الإصلاح الأساسي والوحيد المطلوب لحل "الاختبار لا يظهر للطالب" — **مفيش أي داعي لإعادة
إنشاء الاختبارات القديمة**؛ كل الاختبارات الموجودة بالفعل في Firebase (من قبل هذا التحديث)
هتظهر للطلاب فورًا بعد رفع هذا التحديث، لأنها كانت متخزنة صح من الأساس.

## تحسين إضافي طبّقته (كان مطلوب صراحة في رسالتك)
بعد ما تأكدت إن منطق "درجة النجاح تفتح الدرس التالي" كان **موجود وشغال صح بالفعل** في
`app.js` (فحصته بالتفصيل — حساب النسبة، مقارنتها بـ `averageGrade`، قفل/فتح الدرس التالي)،
لقيت نقطتين ناقصتين بالظبط زي ما وصفتهم:

1. **الطالب الناجح كان لسه يقدر "يعيد" الاختبار** بالضغط على "Retake Quiz" من غير أي مانع.
   → دلوقتي: لو الطالب عدّى درجة النجاح، مش هيقدر يفتح صفحة حل الاختبار تاني خالص — هيتوجّه
   تلقائيًا لصفحة **"مشاهدة النتيجة والإجابات فقط"** (بدون إمكانية تغيير أي إجابة)، وزرار
   "Retake" بيختفي من نافذة النتيجة كمان بعد النجاح مباشرة.
2. **قفل "إتمام الكورس"**: كان زرار "🎉 Congratulations! Course Completed" بيظهر دايمًا في
   آخر درس حتى لو مكنش فيه اختبار الدرس ده اتحل/اتنجح فيه. دلوقتي بيتطبّق عليه نفس بالظبط
   منطق قفل "الدرس التالي" — يعني لو آخر درس مرتبط باختبار ولسه مانجحش فيه، "إتمام الكورس"
   يفضل مقفول لحد ما ينجح.

## الملفات اللي اتعدّلت (اتنين بس)
- **`dashboard-bridge.js`** — إضافة دالة المزامنة الجديدة `startQuizzesFirebaseSync()` فقط.
- **`app.js`** — 4 تعديلات محددة:
  1. سطر واحد لتعريض `window.handleRoute` (تحتاجه المزامنة الجديدة لتحديث الشاشة تلقائيًا).
  2. دالة جديدة `renderAlreadyPassedQuizPage()` (شاشة "شاهد نتيجتك" بعد النجاح).
  3. دالتين مساعدتين (`getLessonQuizGateInfo`, `renderLockedNavButton`) لتوحيد منطق القفل
     بين "الدرس التالي" و"إتمام الكورس" بدل تكراره.
  4. تعديل بسيط في نص وزرار "معاينة الاختبار" داخل الدرس ليعكس حالة "ناجح ✅" بوضوح.

**مفيش أي تعديل في `dashboard.html` خالص** — نظام إنشاء الاختبارات في الداشبورد مكنش فيه أي
مشكلة، فمكانش محتاج أي لمسة.

## قايمة الاختبار (بخصوص الـ 14 خطوة اللي طلبتها)
هذا اللي أقدر أتأكد منه بمراجعة الكود (بدون Firebase حقيقي متاح عندي هنا للتجربة المباشرة)،
وأنصحك تتأكد بنفسك بعد الرفع من الترقيم الفعلي:

| # | الخطوة | الحالة |
|---|--------|--------|
| 1-4 | إنشاء اختبار عادي/AI + وجود ID + ربطه بدرس | ✅ كانت شغالة أصلاً، ماتلمستش |
| 5-6 | دخول الطالب + ظهور الاختبار بعد الدرس | ✅ **تم الإصلاح** (كانت المشكلة الأساسية) |
| 7-8 | درجة أقل من النجاح → الدرس التالي يفضل مقفول | ✅ كان المنطق موجود، هيشتغل دلوقتي لأن البيانات وصلت |
| 9-11 | إعادة المحاولة → نجاح → فتح الدرس التالي | ✅ نفس الشيء |
| 12 | الناجح ما يقدرش يعيد الاختبار | ✅ **تم الإصلاح** (كان ناقص) |
| 13 | مشاهدة النتيجة/الإجابات للناجح | ✅ **تم الإصلاح** (شاشة جديدة مخصصة لكده) |
| 14 | الحالة محفوظة بين الأجهزة (Login من جهاز تاني) | ✅ محفوظة أصلاً في Firebase (`quiz_attempts`) — ماتلمستش |

**مهم:** لازم تجرب الرفع على جهازين مختلفين فعليًا (أو متصفح عادي + متصفح Incognito) للتأكد
من نقطة 5-6، لأن ده بالظبط السيناريو اللي كانت المشكلة بتظهر فيه ومش هيظهر لو جرّبت من نفس
جهاز/متصفح المعلم.

---

## تحديث سريع — إظهار الاختبار كخطوة منفصلة في قائمة الدروس

بعد رفع الإصلاح الأول أكّدت لي (بصورة) إن رسالة القفل وزرار "Start Quiz" بقوا شغالين صح فوق
الصفحة 🎉 — ده كان بالظبط الإصلاح الأساسي. لكن لاحظت إن الاختبار لسه مش ظاهر كـ"صف" منفصل
جوه قائمة "All Course Lectures & Topics" وجوه القائمة الجانبية (Sidebar) — كانت بس بتوريك
الدرس نفسه، والاختبار موجود بس جوه تاب "Quiz & Practice" ورسالة القفل.

أضفت الاختبار الآن كصف منفصل بعد كل درس مرتبط باختبار، في القائمتين مع بعض:
- **القائمة الجانبية** (Sidebar) على يمين الصفحة.
- **قائمة "All Course Lectures & Topics"** أسفل الصفحة (نفس القائمة في الصورة اللي بعتّها).

شكل الصف: `↳ 📝 Quiz: [عنوان الدرس]` مع حالة واضحة (🔒 مقفول / 📝 متاح / ✅ ناجح) حسب حالة
الطالب الفعلية — بدون أي تغيير في بيانات الدروس نفسها أو منطق القفل، مجرد صف إضافي للعرض فقط.

**ملحوظة:** نفس القائمة دي بالظبط موجودة كمان في صفحة "تفاصيل الكورس" (قبل ما الطالب يفتح أي
درس) كمعاينة سريعة — سبتها زي ما هي دلوقتي لأنها معاينة تعريفية بس مش جزء من مسار الدرس
الفعلي، قولّي لو عايزني أضيف نفس الشكل هناك كمان.
