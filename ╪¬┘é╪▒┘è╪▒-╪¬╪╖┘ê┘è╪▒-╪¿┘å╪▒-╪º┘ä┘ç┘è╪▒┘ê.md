# تقرير تطوير بنر الهيرو (v4)

## اللي اتغيّر
- **style.css**: بلوك واحد في آخر الملف اسمه `HERO v4` (للتراجع احذفه بالكامل).
- **templates.js / app.js**: `dir="ltr"` على معادلات الخلفية عشان `∑F = 0` ما تنعكسش.
- **version.json / index.html / dashboard.html / sw.js**: رقم إصدار جديد `20260924-1200` (شغّل update-version.sh قبل النشر لو عايز توقيت حقيقي).

## المرحلة 1 — إصلاحات
1. المعادلات: `direction:ltr` + `unicode-bidi:isolate`.
2. حواف المستطيل حوالين الصورة: الـ mask كان بيقص الـ drop-shadow، فاتضافت مساحة شفافة حوالين الصورة داخل صندوق الـ mask.
3. الزرار فوق الطية: ديسكتوب (≥1025px) بيتناسب مع ارتفاع الشاشة؛ موبايل/تابلت ترتيبه الاسم ← الشعار ← الزراير ← الصورة (مقصوصة عند الصدر).

## المرحلة 2 — الألوان الثانوية
- عنبري `#FFB627`: زرار الإجراء في الليلي + نقطة الـ eyebrow + شرطة الشعار.
- سيان: خط الإضاءة على حافة الصورة (نهاري وليلي) + الإلكترونات الموجودة أصلًا في `--hb-cy`.
- زرار الإجراء نهاري: بنفسجي غامق `#5B33A8` (7.3:1 مع الأبيض 8.4:1).
- توكنز محجوزة للمرحلة 3: `--hb-spark-text` و`--hb-cy-text`.

## ملاحظات
- لم يتم دمج طبقات الـ hero القديمة في style.css (عمدًا، لأن التعديل بدون رندر خطر)؛ البلوك الجديد بيتغلّب عليها بالترتيب.
- لم يتم اختبار بصريًا في متصفح. اختبر: 360×740 و390×844 و768×1024 و1366×640 و1536×730 و1920×1080، نهاري وليلي.
- رقم واتساب `201000000000` placeholder ولسه موجود.
