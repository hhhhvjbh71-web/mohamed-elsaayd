# تقرير إصلاح نظام الاختبارات — من الـ Dashboard إلى الطالب

> النطاق: إصلاح النظام الموجود وربطه، بدون بناء نظام جديد وبدون تغيير تصميم الـ Dashboard أو بياناته.
> ملاحظة: التقرير القديم `تقرير-إصلاح-نظام-الاختبارات.md` ادّعى أن المحاولات محفوظة ومتزامنة عبر Firebase؛ هذا **غير صحيح** (سببه في القسم 1، البند 3).

## 1) لماذا لم يكن الاختبار يعمل؟ (أسباب جذرية وُجدت بالفحص)

1. **`data.js` كان يتوقف عن التنفيذ عند سطر واحد:** `window.COURSES_DATA = COURSES_DATA` والمتغير غير معرّف في المشروع (خطأ `ReferenceError` في الأصل). كل ما بعده في الملف لم يُنفَّذ أبداً، ومنه **نظام التقدّم كله**: `markLessonCompleted` و`getLessonAccessStatus` و`enrichLessonsWithProgress`. النتيجة: قفل الدرس التالي بعد الاختبار لم يكن يعمل من الأساس.
2. **صفحة اختبار الطالب لا تعمل:** الكود كان داخل `<script>` في `innerHTML`، والمتصفحات لا تنفّذ السكربتات المُدرجة بهذه الطريقة. الصفحة كانت تظهر لكن لا يمكن اختيار إجابة ولا يعمل المؤقت ولا التسليم (جرّبتها فعلياً على المتصفح: `selectTestOption is not defined`).
3. **المحاولات تُقرأ من `localStorage` فقط:** كانت تُكتب في Firestore ولا تُقرأ منه، وبصيغة مستند (`userId_quizId`) تختلف عمّا تقرأه الداشبورد (`ATT_studentId_quizId`، `studentName`، `totalPoints`)، وكل محاولة جديدة تمسح السابقة بلا عدّ.
4. **الإجابات الصحيحة كانت تصل إلى كل طالب** مع بيانات الاختبار (وتُخزَّن في متصفحه).
5. **الداشبورد كانت تولّد نتائج وهمية** لأي اختبار بلا محاولات "لأغراض التجربة" وتعرضها كأنها حقيقية — أُزيلت.

## 2) دورة البيانات بعد الإصلاح

Dashboard ← إنشاء الاختبار ← Test ID ← ربطه بالدرس ← حفظ الربط في Database ← الطالب يفتح الكورس ← يرى الدرس ← يظهر الاختبار ← صفحة الاختبار ← بدء المحاولة ← الحل ← الإنهاء ← التصحيح ← حفظ النتيجة ← صفحة النتيجة ← فتح/قفل الدرس التالي.

| المرحلة | ما يحدث الآن |
|---|---|
| إنشاء الاختبار | يُحفظ في `quizzes` (كاملاً بالإجابات) + نسخة الطالب `quizzes_public` **بدون الإجابات**. حقلان جديدان في نافذة الاختبار: «عدد المحاولات المسموحة» و«مراجعة الإجابات الصحيحة». |
| ربط Test ID بالدرس | يُحفظ في `lesson.quizId` داخل `platform_data/courses_list` كما كان. أُضيف تحقق من قاعدة البيانات: الاختبار لازم يكون **موجوداً فعلاً** ويخص الحساب (`ownerId`) وإلا يُرفض الربط. |
| ظهور الاختبار للطالب | الصفحة تفتح فقط إذا كان الاختبار مربوطاً بهذا الدرس داخل بيانات الكورس، والدرس غير مقفول، والكورس مفعّل. كتابة ID في الرابط لا تكفي. |
| صفحة الاختبار (الطالب) | اسم الاختبار، الوصف، عدد الأسئلة، المدة، درجة النجاح، المحاولات، الحالة، التعليمات، زر البدء. |
| المؤقت | وقت البدء = `serverTimestamp` في Firestore؛ الوقت المتبقي يُحسب من ساعة السيرفر (مع ساعة رتيبة تتجاهل تغيير ساعة الجهاز). يستمر رغم Refresh أو إغلاق المتصفح أو تغيير الجهاز. |
| انتهاء الوقت | تسليم تلقائي وتصحيح وحفظ من الإجابات المحفوظة. لو كان الطالب غائباً يُنهى تلقائياً عند أول عودة. |
| محاولة واحدة/عدد محدد | يُفرض داخل Transaction على مستند `ATT_studentId_quizId`: Refresh / Logout-Login / نافذة جديدة / جهاز أو متصفح آخر كلها ترى نفس الحالة. بدء مرتين في نفس اللحظة = محاولة واحدة. |
| أثناء الحل | الإجابات تُحفظ تلقائياً في Firestore، ولا تُجلب الإجابات الصحيحة إطلاقاً قبل التسليم، ولا تظهر النتيجة. |
| النتيجة | تُحفظ: الإجابات، الدرجة، الإجمالي، النسبة، الصحيح، الخطأ، وقت البدء، وقت الانتهاء، المدة، `passed`، سبب الإنهاء، وتاريخ المحاولات `history`. |
| صفحة النتيجة | الاسم، الدرجة، الإجمالي، النسبة، الصحيح، الخطأ، النجاح/الرسوب، وقت الانتهاء. الإجابات الصحيحة **لا تظهر إلا إذا فعّلت «مراجعة الإجابات»** للاختبار. |
| قفل الدرس التالي | يُحدَّد من النتيجة المحفوظة في Firestore (مستمع لحظي) وليس من حالة مؤقتة في المتصفح. جهاز جديد يصل لنفس الحالة تلقائياً. |

## 3) هيكل البيانات
- `quiz_attempts/ATT_{studentId}_{quizId}` (نفس صيغة الداشبورد): `status` (`in_progress`/`submitted`)، `attemptsUsed`، `maxAttempts`، `currentRun{startedAt(server), durationSec, answers}` أثناء الحل، ثم نتيجة كاملة + `history[]`. المستندات القديمة (`userId_quizId`) تُقرأ وتُحتسب محاولة مستخدمة ولا تضيع نتائجها.
- `quizzes_public/{id}` جديدة (نسخة الطالب بلا إجابات)، و`quiz_clock/{studentId}` جديدة (مزامنة ساعة السيرفر).
- تعبئة `quizzes_public` للاختبارات القديمة تحدث تلقائياً **أول مرة تفتح فيها الداشبورد** (وحتى ذلك الحين الطالب يستلم نسخة مجرّدة من الإجابات محلياً).

## 4) تعديلات في الداشبورد (إضافية فقط)
حقلا الإعداد، الكتابة في `quizzes_public` عند الحفظ/النسخ/الحذف، التحقق عند ربط الاختبار بالدرس، توحيد قراءة النتائج (الصيغتان القديمة والجديدة)، احتساب الإحصاءات من المحاولات المنتهية فقط، إصلاح «إعادة تعيين المحاولة» ليحذف المستندين، وإزالة النتائج الوهمية.

## 5) ⚠️ تغييرات سلوك يجب أن تعرفها قبل النشر
1. **قفل الدروس سيبدأ يعمل فعلاً** (كان معطّلاً بسبب خطأ `data.js`). طالب لم ينجح في اختبار درس سيجد الدرس التالي مقفولاً. استخدم «إعادة تعيين المحاولة» أو غيّر درجة النجاح إن لزم.
2. **الإجابات الصحيحة صارت مخفية افتراضياً** بعد الاختبار (كانت تظهر دائماً). فعّل «مراجعة الإجابات» لكل اختبار تريد لطلابه مراجعتها.
3. **الاختبارات القديمة** بلا `maxAttempts` تعمل كما كانت: إعادة المحاولة مسموحة حتى النجاح. اضبط «محاولة واحدة» من الداشبورد حيث تريد.
4. الأزرار/الصفحة بالإنجليزية لتتوافق مع بقية واجهة الطالب.

## 6) الاختبارات التي أُجريت
68 فحصاً آلياً على متصفح حقيقي (Chromium): محاولة واحدة، Refresh، جهاز آخر بساعة منحرفة 10 دقائق، استئناف الإجابات، إنهاء ونتيجة، منع الإعادة (واجهة وAPI مباشر)، إعادة محاولة ثم نجاح ثم فتح الدرس التالي على جهاز جديد، انتهاء الوقت أثناء الحل وبعد الغياب، بدء/تسليم متزامنين، إخفاء/إظهار الإجابات، بيانات قديمة، رفض اختبار غير مربوط أو درس مقفول، ولوحة التحكم (نشر، إعدادات، نسخ، حذف، نتائج حقيقية فقط، إعادة تعيين، رفض ربط ID غير موجود أو يخص حساباً آخر).

**حدّ الاختبار:** بيئتي بلا وصول إلى Firebase، فالفحوص على **Firestore محاكى** يدعم Transactions بالتعارض المتفائل وServer Timestamp. لم أجرّب على Firebase الحقيقي. جرّب على نسخة تجريبية بجهازين قبل النشر.

## 7) حدود أمنية صريحة (مهم لأنك تشترط أماناً صارماً)
المنصة لا تستخدم Firebase Auth ولا يوجد سيرفر؛ كل شيء يعمل من متصفح الطالب مباشرة على Firestore. لذلك:
- ما تم منعه: التحايل العادي (Refresh، تسجيل الخروج، جهاز/متصفح آخر، فتح الأدوات بسهولة، ظهور الإجابات في المتصفح/الشبكة قبل التسليم، تلاعب ساعة الجهاز).
- ما لا يمكن منعه بدون سيرفر: مطوّر متمرّس يستطيع مخاطبة Firestore مباشرة (قراءة `quizzes` بالإجابات، أو كتابة مستند محاولة)، لأن القواعد لا تعرف هوية الطالب.
- **الحل النهائي:** Firebase Auth (طالب/مدرس) + Cloud Function للبدء والتصحيح (الإجابات في مجموعة لا يقرؤها الطالب) + قواعد Firestore تمنع كتابة النتائج من العميل. لم أنفّذه لأنه يتطلب بنية جديدة وخطة Blaze، وطلبك كان إصلاح الموجود. أستطيع تجهيزه كمرحلة قادمة.

## 8) خطوات النشر
1. ارفع كل الملفات (النسخة `20260921-0346`؛ جديد: `quiz-service.js` و`quiz-ui.js`).
2. افتح الداشبورد مرة واحدة بحساب المدرس (يُنشر `quizzes_public`).
3. لو قواعد Firestore عندك تحدّد المجموعات بالاسم، أضف: `quizzes_public` (قراءة للجميع، كتابة للمدرس) و`quiz_clock` (كتابة/قراءة). ومستند `quiz_attempts` يستمر كما هو.
4. اضبط «عدد المحاولات» و«مراجعة الإجابات» للاختبارات القائمة.
5. جرّب بحساب طالب على جهازين: ابدأ اختبارًا ثم أغلق المتصفح وعد من جهاز آخر.

## 9) لم أنفّذه
إنهاء المحاولات المنتهية تلقائياً من جهة الداشبورد (تُنهى عند عودة الطالب؛ حتى ذلك الحين تظهر «معلّقة» في نتائج المدرس)، وسجل ربط مستقل Course/Lesson/Test (الربط محفوظ في `lesson.quizId` داخل بيانات الكورس ويُتحقق منه عند الفتح).
