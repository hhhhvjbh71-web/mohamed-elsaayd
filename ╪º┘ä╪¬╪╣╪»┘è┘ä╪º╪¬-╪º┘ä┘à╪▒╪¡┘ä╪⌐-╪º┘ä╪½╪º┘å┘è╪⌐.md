# تقرير المرحلة الثانية — تحويل المنصة إلى PWA حقيقية قابلة للتثبيت

هذا الملف بيوثّق كل تعديل حصل بالضبط لتفعيل الـ PWA، عشان تقدر تراجعه وتختبره بنفسك قبل النشر.
تم اتباع نفس منطق تقرير المرحلة الأولى: **إضافة فوق النظام الحالي، من غير حذف أو إعادة بناء لأي حاجة موجودة.**

## الملفات الجديدة (إضافة بحتة)
- `manifest.json` — بيانات التطبيق (الاسم، الأيقونات، الألوان، وضع Standalone)
- `sw.js` — الـ Service Worker (تسجيل + كاش + تحديث تلقائي)
- `pwa-install.js` — تسجيل الـ Service Worker + منطق نافذة التثبيت
- `pwa-install.css` — تصميم نافذة التثبيت (Bottom Sheet / Modal)
- `offline.html` — صفحة احتياطية بسيطة تظهر فقط لو المستخدم فتح صفحة غير محفوظة وهو Offline
- `favicon.ico` (في جذر المشروع) + ملفات الأيقونات بكل المقاسات المطلوبة (في جذر المشروع أيضًا):
  `icon-192.png`, `icon-512.png`, `icon-maskable-192.png`, `icon-maskable-512.png`,
  `apple-touch-icon.png` (180×180), `favicon-32.png`, `favicon-16.png`

## الملفات اللي اتعدّلت (تعديل محدود ومحسوب)
- `index.html` — إضافة روابط الـ manifest/الأيقونات/theme-color في `<head>`،
  وإضافة `<script src="pwa-install.js">` بعد `cache-buster.js` مباشرة (بدون تغيير ترتيب باقي السكربتات)
- `dashboard.html` — نفس إضافات `index.html` بالظبط (head + script في الآخر بعد cache-buster.js)
- `firebase.json` — إضافة قاعدتين Headers جديدتين بس (لـ `/sw.js` و `/manifest.json`)، من غير حذف أو تعديل أي قاعدة موجودة
- `update-version.sh` / `update-version.ps1` — امتداد بسيط: نفس السكربت بتاعك دلوقتي كمان
  بيحدّث `?v=` في `dashboard.html` (كان بيحدّثها في `index.html` بس)، وبيحدّث رقم إصدار `sw.js` تلقائيًا
- `version.json` — تم رفع رقم الإصدار (تلقائيًا عن طريق تشغيل السكربت كاختبار نهائي، موضح في القسم 5)

## الملفات اللي **متغيرتش خالص** (زي ما هي 100%)
`.htaccess`, `app.js`, `cache-buster.js`, `dashboard-bridge.js`, `data.js`,
`firebase-config.js`, `firebase-service.js`, `style.css`, `templates.js`,
كل الصور الأصلية (`hero-teacher.jpg`, `teacher-new.jpg`), `sitemap.xml`.

مفيش أي تعديل في: منطق تسجيل الدخول، نظام أكواد التفعيل، الصلاحيات، قاعدة البيانات،
أو أي إعدادات Firebase.

---

## 1) إزاي النظام الجديد بيتعايش مع `cache-buster.js` الموجود عندك
المنصة عندها بالفعل نظام تحديث ذكي (`cache-buster.js`) بيراقب `version.json` كل دقيقتين
+ عند رجوع المستخدم للتاب + عند رجوع الإنترنت، ولو لقى نسخة جديدة بيمسح الكاش ويعمل Reload.
تم تصميم الـ Service Worker الجديد **عشان يشتغل معاه مش بدله**:

- `version.json` نفسه **ممنوع تمامًا** يتحفظ في أي كاش أو يتم اعتراضه — بيعدي للنتورك زي
  ما هو دايمًا، عشان `cache-buster.js` يفضل شغال 100% زي ما هو مصمم من غير أي تغيير في سلوكه.
- كل ملفات نفس الموقع (HTML/JS/CSS) بتتعامل معاها استراتيجية **Network First**: أول حاجة
  يتحاول يجيبها من السيرفر مباشرة، ولو الإنترنت اتقطع يرجع للكاش كـ Backup بس مش كمصدر أساسي.
  ده معناه إن أي تحديث ترفعه بيوصل فورًا لأي حد عنده إنترنت، بغض النظر عن الـ Service Worker خالص.
- الصور والخطوط ومكتبات الـ CDN الثابتة (Firebase SDK، pdf.js، Google Fonts) بس هي اللي بتتعامل
  معاها استراتيجية **Cache First** لأنها تقيلة ونادرًا ما تتغير (ومعظمها مثبّت برقم إصدار في الرابط أصلاً).

النتيجة: الـ Service Worker بيضيف تجربة تثبيت + سرعة فتح + دعم بسيط للأوفلاين، من غير ما
يبقى "طبقة تانية من الكاش" ممكن تسبب ظهور نسخة قديمة.

## 2) نافذة التثبيت
- بتظهر في **كل صفحة** (`index.html` و `dashboard.html`) طول ما التطبيق لسه مش متثبّت،
  وبتختفي نهائيًا وبشكل دائم بعد نجاح التثبيت فعليًا (مش مجرد الضغط على "ليس الآن").
  زر "ليس الآن" **مبيسجّلش أي رفض دائم** — النافذة هتظهر تاني في أي زيارة جديدة، بالظبط
  زي ما طلبت في نقطة 2.
- على `index.html` بتستنى شاشة اللودينج بتاعتك تخلص الأول (بتراقب اختفاءها فعليًا، مش تايمر ثابت)
  عشان ميحصلش تداخل بصري.
- بتكتشف نوع الجهاز/المتصفح:
  - **Chrome / Edge / Android** (بيدعموا `beforeinstallprompt`): زرار "تثبيت التطبيق الآن" شغال فعليًا.
  - **iPhone/iPad (Safari)**: مفيش `beforeinstallprompt` في iOS خالص، فبتظهر تعليمات مصوّرة
    (مشاركة ⬆️ ← "إضافة إلى الشاشة الرئيسية").
  - **macOS Safari**: تعليمات "إضافة إلى الرصيف" (بونص إضافي مش في الطلب الأصلي بس مفيد).
  - **أي متصفح تاني مش بيدعم التثبيت** (زي Firefox Desktop حاليًا): **مفيش أي نافذة بتظهر خالص** —
    بدل ما تظهر زرار مايشتغلش، بالظبط زي ما طلبت في نقطة 15.
- الشكل: Bottom Sheet على الموبايل / Modal في النص على الديسكتوب، مع Animation خفيفة،
  ودعم كامل للوضع الليلي (`[data-theme="dark"]`) على نفس نمط `dashboard.html`.

## 3) الأيقونات
مفيش صورة "لوجو مربّع" جاهزة في المشروع أصلاً — الشعار الحقيقي للمنصة هو رسمة SVG (حرف π
بتدرّج ذهبي + حلقة مدارية) موجودة جوه `app.js` وبتظهر في هيدر الموقع. تم استخدام **نفس الرسمة
دي بالظبط** (من غير أي تغيير في ألوانها) فوق خلفية كحلي (`#0F172A`، من نفس باليتة المنصة)
لعمل مجموعة الأيقونات كاملة: `192`, `512`, نسخة Maskable لأندرويد (بمساحة أمان كافية جوه
الدائرة اللي بيقصّها النظام)، `apple-touch-icon` لآيفون، و`favicon` للمتصفح. الهوية البصرية
للمنصة **متغيّرتش خالص** — استخدام حرفي لنفس الشعار الموجود.

## 4) ربط رقم الإصدار (Versioning)
تم اتباع نفس نظامك الحالي **بدل ما نخترع نظام جديد**:
- `sw.js` فيه سطر واحد بس بيحمل رقم النسخة: `const APP_VERSION = '...'` بنفس صيغة
  `version.json` (`YYYYMMDD-HHMM`)، وهو اللي بيتحدّث تلقائيًا مع كل تشغيل للسكربت.
- لما محتوى `sw.js` يتغيّر فعليًا (لأن الرقم نفسه اتغيّر)، المتصفح بيكتشف كده تلقائيًا إن فيه
  نسخة جديدة من الـ Service Worker ويعمل `skipWaiting()` + `clients.claim()` (بالظبط الآليات
  اللي طلبتها في نقطة 7) — يعني التحديث بياخد مفعوله فورًا من غير ما تحتاج تقفل كل التابات.
- تم تشغيل `update-version.sh` فعليًا كاختبار نهائي أثناء التنفيذ، وأكّد إنه بيحدّث الأربعة
  ملفات مع بعض بنجاح: `version.json`, `index.html`, `dashboard.html`, `sw.js`.

## 5) `firebase.json` — تعديل واحد ضروري
كانت عندك قاعدة عامة `**/*.@(js|css)` بتدي كل ملفات الجافاسكريبت والـ CSS كاش لمدة **سنة كاملة**
(`immutable`). ده ممتاز لملفات زي `app.js`/`style.css` لأنها بتتحمّل برابط فيه `?v=` بيتغيّر مع
كل نسخة، لكنه كان هيمنع **`sw.js` نفسه** من اكتشاف أي تحديث بسرعة (لأن رابطه ثابت من غير `?v=`،
والمتصفح مش هيروح يجيب نسخة جديدة منه لحد ما يعدي سنة كاملة تقريبًا). عشان كده تم إضافة قاعدتين
جديدتين بس (**بعد** القاعدة العامة عشان تتفوّق عليها لهذين الملفين تحديدًا):
- `/sw.js`: `no-cache, no-store, must-revalidate` — عشان يوصل فورًا مع كل رفعة جديدة.
- `/manifest.json`: نفس المعاملة، تحسبًا لو غيّرت اسم/أيقونة التطبيق مستقبلًا.

باقي القواعد (الصور، الخطوط، `index.html`, `version.json`) **متغيرتش ولا حرف فيها**.

## 6) صفحة `offline.html`
صفحة بسيطة ومستقلة (بدون أي اعتماد على `style.css` أو خطوط خارجية) بتظهر بس في حالة إن
المستخدم فتح صفحة جديدة تمامًا وهو أوفلاين ومفيش نسخة محفوظة منها. لو الصفحة كانت اتفتحت
قبل كده وهو أونلاين، هيشوف آخر نسخة محفوظة منها بدل الصفحة دي.

---

## قايمة اختبار (مطابقة لنقطة 17 في الطلب)
- [ ] الموقع لسه شغال طبيعي 100% زي الأول (تسجيل دخول، حسابات، دروس، اختبارات، أكواد تفعيل)
- [ ] نافذة التثبيت بتظهر لمستخدم غير مثبّت (Chrome/Edge على أندرويد أو ديسكتوب)
- [ ] بعد الضغط على "تثبيت التطبيق الآن" وقبول التثبيت → التطبيق يظهر باسم **منصة مستر محمود عبد الدايم**
- [ ] بعد التثبيت، النافذة **مبتظهرش تاني** حتى لو رجعت تفتح الموقع من المتصفح العادي
- [ ] فتح التطبيق المثبّت بيشتغل Standalone (من غير شريط عنوان المتصفح)
- [ ] على آيفون (Safari): تظهر تعليمات "إضافة إلى الشاشة الرئيسية" بدل زرار تلقائي
- [ ] `manifest.json` و `sw.js` بيتحمّلوا من غير أخطاء في الـ Console
- [ ] Firebase (تسجيل دخول / بيانات / اختبارات) شغالة زي الأول من غير أي تغيير
- [ ] بعد رفع تحديث جديد وتشغيل `update-version.sh`، التحديث بيوصل للمستخدم من غير ما يحذف التطبيق
- [ ] لا يحدث أي تسجيل خروج أو فقد بيانات بسبب تحديث الـ PWA

## ملاحظات وحدود معروفة (مهم تعرفها)
- **Firefox Desktop** حاليًا مبيدعمش تثبيت الـ PWA خالص، فمش هتظهر أي نافذة فيه (بدل زرار مايشتغلش).
- الفيفيكون الصغير (16×16) بيفقد بعض التفاصيل الدقيقة في الشعار (طبيعي في أي أيقونة بهذا الحجم الصغير).
- `dashboard.html` مش بيرفع `?v=` تلقائيًا في السكربت القديم — تم توسيع `update-version.sh/.ps1`
  ليغطيه كمان (تحسين إضافي بسيط، من غير أي حذف لسلوك موجود).

## المرحلة الجاية (لو عايز نكمل)
1. إضافة Shortcuts داخل الـ manifest (زي اختصار مباشر لـ "لوحة التحكم" من الضغط المطوّل على الأيقونة).
2. مزامنة مفتاح تخزين الوضع الليلي بين `index.html` (`iraqiplatform_theme`) و`dashboard.html`
   (`alsaqr-theme`) — لاحظنا إنهم مختلفين حاليًا، مش جزء من طلب الـ PWA لكن يستاهل مراجعة.
3. إشعارات Push حقيقية (لو حبيت تنبّه الطلاب بدرس/اختبار جديد) — محتاجة إعداد Firebase Cloud
  Messaging بشكل منفصل، ومفيش أي حاجة من كده دلوقتي.

---

## تحديث سريع (بعد المراجعة) — الأيقونة + اسم التطبيق بالإنجليزي

- **الأيقونة:** اتغيّرت من شعار الـ π لصورة المستر اللي بعتّها. تم عمل نسخة عادية (Full-Bleed) لكل
  المقاسات (`192`, `512`, `apple-touch-icon`)، ونسخة تانية خاصة بـ Maskable (`icon-maskable-*`)
  بخلفية من نفس الصورة مموّهة عشان الوش يفضل واضح جوه "منطقة الأمان" حتى لو النظام قصّ الأيقونة
  بشكل دائري (بعض أجهزة الأندرويد بتعمل كده). الفيفيكون الصغير (`favicon-16/32`) اتعمله تقريب
  (Crop) على الوش بس عشان يفضل واضح في الحجم الصغير جدًا.
- **اسم التطبيق:** اتغيّر بالكامل للإنجليزي في `manifest.json` (`name`, `short_name`,
  `description`) وفي meta tag الخاص بـ iOS (`apple-mobile-web-app-title`) في الصفحتين. الاسم
  المختصر اللي هيظهر تحت الأيقونة: **"Mr. Abdeldayem"**.
- عنوان نافذة التثبيت (اللي بتظهر للمستخدم) اتغيّر هو كمان للإنجليزي بالكامل
  ("📱 Install Mr. Mahmoud Abdeldayem Platform") عشان يطابق اسم التطبيق الجديد، ووصف النافذة
  وباقي الأزرار سابتهم عربي زي ما هي (مجرد نص إرشادي مش اسم).
- تم تشغيل `update-version.sh` تاني بعد التعديل ده — النسخة الحالية: راجع `version.json`.

---

## تحديث سريع 2 — تبسيط الأيقونة + تغيير مشروع Firebase بالكامل

### 1) الأيقونة
شلت المعالجة الفنية اللي كانت جوه نسخة الـ Maskable (الخلفية المموّهة). دلوقتي **كل** مقاسات
الأيقونة (`192`, `512`, `maskable-192`, `maskable-512`, `apple-touch-icon`, والفيفيكون) هي
نفس صورة البانر اللي بعتّها **بدون أي تعديل أو تركيب** — مجرد تصغير للمقاس المطلوب بس.

### 2) تغيير مشروع Firebase بالكامل — ⚠️ مهم جدًا تقرأه قبل الرفع
استبدلت بيانات الاتصال بالكامل في الأماكن الأربعة اللي كانت فيها (كل الملفات التانية متعرفش
أصلاً على أي بيانات Firebase، فمفيش مكان اتنسى):
- `firebase-config.js`
- `firebase-service.js`
- `index.html` (روابط canonical / og:url / JSON-LD كانت بتشاور على دومين المشروع القديم)
- `sitemap.xml` (نفس الموضوع)

المشروع بقى `mahmoud-abdeldaem-manassa` بدل `siond-a6c34`، ودومين الموقع في كل الروابط بقى
`https://mahmoud-abdeldaem-manassa.web.app/`.

**⚠️ نقطة لازم تاخد بالك منها قبل الرفع:** المشروعين دول في Firebase منفصلين تمامًا عن بعض —
مش نفس القاعدة بس باسم جديد. يعني:
- أي حسابات مستخدمين/طلاب متسجّلة دلوقتي في مشروع `siond-a6c34` **مش هتكون موجودة** في مشروع
  `mahmoud-abdeldaem-manassa` الجديد، إلا لو نقلتها بنفسك (Export/Import) قبل كده.
- نفس الكلام على أي بيانات في Firestore (كورسات، دروس، أكواد تفعيل، نتائج اختبارات) —
  المشروع الجديد فاضي لحد ما تحطّ فيه البيانات دي بنفسك.
- لو فيه ملفات متخزّنة على Firebase Storage (مش روابط خارجية)، هي كمان مش هتتنقل تلقائيًا.
- لازم تظبط Firestore Rules و Storage Rules على المشروع الجديد بنفسك من Firebase Console
  (الملفات اللي معايا متضمنش الـ Rules، بتتظبط من الكونسول أو عن طريق CLI منفصل).
- ملف `.firebaserc` (اللي بيربط أمر `firebase deploy` بمشروع معيّن) مش موجود ضمن الملفات
  اللي بعتّهالي، فلازم تعدّله بنفسك (أو تشغّل `firebase use --add` وتختار المشروع الجديد)
  قبل أي `firebase deploy` جديد، وإلا هيرفع على المشروع القديم بالغلط.

لو المشروع الجديد ده أصلاً معمول له نفس الإعداد (Rules + بيانات منقولة) فالتعديل الكودي ده كافي
ومفيش حاجة تانية تعملها. لو لسه مش عامل الخطوات دي، يفضل تتأكد منها الأول قبل ما ترفع التحديث،
عشان تتفادى إن الطلاب الحاليين يلاقوا نفسهم مش قادرين يسجّلوا دخول فجأة.

### 3) بخصوص `version.json`
موجود في جذر المشروع (`/version.json`)، ورقمه بيظهر كمان كـ `?v=...` في نهاية روابط كل ملفات
الـ JS/CSS داخل `index.html` و `dashboard.html`. لو هتغيّره يدويًا، لازم تغيّره في **كل الأماكن
دي مع بعض** (والسطر بتاع `APP_VERSION` جوه `sw.js` كمان) — أو الأسهل تشغّل `update-version.sh`
(أو `.ps1` على ويندوز) وهو بيعمل ده كله تلقائيًا في ثانية واحدة، وهو بالظبط اللي عملته دلوقتي
بعد كل التعديلات اللي فوق.


